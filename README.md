# Portal of Collective Imagination — WebGL MVP

WebGL implementation of the Display Mac compositor, used to validate
that browsers can drive 8K output through the Fresco splitter chain.

Sibling project to `tunnel-mvp` (the Unity implementation).
